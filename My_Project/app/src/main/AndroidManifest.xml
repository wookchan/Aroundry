<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.my_project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.My_Project"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".President.HW_Popup3"
            android:theme="@style/notitledidalog" />
        <activity
            android:name=".President.HW_Popup2"
            android:theme="@style/notitledidalog" />
        <activity
            android:name=".President.HW_Popup"
            android:theme="@style/notitledidalog" /> <!-- hw팝업 android:theme="@android:style/Theme.Dialog" -->
        <activity
            android:name=".Loading.LoadingActivity"
            android:exported="true" />
        <activity
            android:name=".Adapter.Setak.HW_Listinformation"
            android:exported="true" />
        <activity
            android:name=".HW_Home"
            android:exported="true" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <!-- <meta-data -->
        <!-- android:name="com.google.android.geo.API_KEY" -->
        <!-- android:value="AIzaSyD4xtIeHldojlxF4Ya8OYouXB4mM25Tpmc" /> -->
        <activity
            android:name=".HW_MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>